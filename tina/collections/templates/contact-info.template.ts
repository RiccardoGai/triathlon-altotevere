import { Template } from 'tinacms';

export const ContactInfoTemplate: Template = {
  name: 'contactInfo',
  label: 'Contact Info',
  fields: [
    {
      name: 'empty',
      type: 'string',
      ui: {
        component: null
      }
    }
  ]
};
